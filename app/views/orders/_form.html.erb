<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :user_id %>
  </div>
  <div class="form-group">
    <%= f.label :party_date %><br>
    <%= f.date_select :party_date %>
  </div>

  <div class="form-group">
    <%= f.label :total_price, "Tray 1 - 24 assorted sandwiches and desserts" %><br>
    <%= f.number_field :total_price, :step => 1.0 %>
  </div>


  <div class="form-group">
    <%= f.label :total_price %><br>
    <%= f.number_field :total_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
